<%- include('../../partials/header.ejs') %>
  <%- include('../../partials/homenavbar.ejs') %>
  <div class="container mt-5">
    <hr>
    <% result.articles.rows.forEach(article => { %>
      <div class="card mt-3">
        <div class="card-header">
          <h2><%= article.title %></h2>
        </div>
        <div class="card-body">
          <a class="btn btn-success" href="/<%= article.slug %>">Ler artigo</a>
        </div>
      </div>
    <% }) %>
    <div class="row mt-3 mb-3">
      <div class="col-12">
        <% if(result.next){ %>
          <a href="/articles/page/<%= result.page + 1 %>" class="btn btn-primary right">Próximo >>></a>
        <% } %>
        <% if(result.page > 1) { %>
          <a href="/articles/page/<%= result.page - 1 %>" class="btn btn-primary"><<< Anterior</a>
        <% } else if (result.page === 1){ %>
          <a href="/" class="btn btn-primary"><<< Anterior</a>
        <% } %>
      </div>
    </div>
  </div>
<%- include('../../partials/footer.ejs') %>
